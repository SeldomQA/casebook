## 0.10.0

- 优化：统一评审标记名称，将工作台和静态导出页面中的 `Needs update` 改为 `Mark`，已有标记数据保持兼容。
- 新增：测试计划记录并按名称列出已生成报告；同名报告再次生成时覆盖原文件和记录，不产生重复条目。
- 优化：当前测试计划按启动范围保存在浏览器中，刷新页面后自动恢复选择；无效或已删除计划自动清理。
- 优化：创建测试计划必须填写名称；名称为空时创建按钮置灰，接口也会拒绝空名称。
- 新增：测试计划执行文件升级为 `schema_version: "2.0"`，通过独立的 `cases` 区域保存计划创建或用例加入计划时的轻量用例快照。
- 优化：执行结果继续独立保存在 `results`，测试报告优先使用用例快照，避免后续 YAML 修改、ID 重排或用例删除影响历史报告。
- 兼容：旧版测试计划 JSON 无需迁移，仍可读取并通过当前 YAML 补充用例信息后生成报告。

## 0.9.0

- 新增：用例 ID 支持点击复制，方便将目标用例准确交给 AI Agent 进行补充、删除或重构。
- 新增：用例列表增加 `Plans` 列，展示每条用例所属的测试计划。
- 新增：用例编辑抽屉支持选择进行中的测试计划，并将现有 YAML 用例加入计划；已完成计划保持只读。
- 新增：计划模式支持重新整理用例 ID，并同步迁移计划范围和既有执行结果，无需手工修改 `test-runs/*.json` 中的 `case_scope`。
- 优化：用例 ID 重排支持同一 YAML 文件包含多种 ID 前缀；每种前缀依据首次出现的编号和位数独立连续排序。
- 优化：ID 重排只迁移计划原本包含的用例，不会将 YAML 中新生成但尚未选择的用例自动加入计划。
- 优化：展开后的用例详情区域自动匹配右侧评审或执行面板高度，减少大屏下的空白与错位。
- 优化：更新工作台、静态用例页、测试计划和测试报告截图，以匹配最新界面。
- 修复：恢复计划归属表头和编辑抽屉中的计划控件，并增加空值保护，避免模板与脚本不一致时页面停止初始化。
- 修复：`casebook serve <目录>` 启动前校验扫描目录；目录不存在、路径无效或目标不是目录时直接输出明确错误并退出。

## 0.8.0

- 新增：完成测试计划时支持输入报告名称并同步生成 HTML 报告，报告默认写入 `reports/`，目录不存在时自动创建。
- 新增：已完成的测试计划支持通过 `Generate report` 重新生成报告，并在抽屉中直接打开生成结果。
- 新增：测试报告采用项目简报式布局，增加执行完成率、风险提醒、Execution Summary、Quality Signals、Attention Required 和 Execution Details。
- 新增：报告执行明细使用可折叠用例列表，默认收起，主行展示 Case、Title、Priority 和 Result，展开后查看完整执行证据。
- 优化：`casebook serve` 整体升级为轻量 SaaS 简报风格，统一顶部概览、卡片、筛选器、状态色和用例表格视觉。
- 优化：工作台取消中间区域最大宽度限制，在大屏下自动平铺；窄屏下保留表头、Priority 和 Actions，并隐藏 Type。
- 优化：文件树隐藏原生滚动条但保留滚动能力，支持拖动调整侧栏宽度。
- 优化：Title 获得更大的表格宽度，Tags 缩窄；Actions 左对齐，并保证 Edit 与执行状态控件在窄屏下可见。
- 优化：计划模式隐藏 Edit 和 ID sorting，避免执行过程中修改用例定义或重排用例 ID。
- 优化：测试计划的选择、创建、完成和报告生成集中到右侧抽屉；主页面保留当前计划摘要和执行统计。
- 优化：未选择测试计划时隐藏进度条和统计卡片，减少评审模式下的界面干扰。
- 优化：计划状态下的执行结果下拉框自动适配 Actions 列宽，保留表格右侧留白。
- 优化：`casebook export` 静态评审页面升级为简报风格，统一项目概览、风险信息、筛选与用例清单布局。
- 优化：测试报告页脚移除蓝色渐变，改为白色卡片式页脚；用例 ID、Priority 和 Result 的视觉层级重新调整。

## 0.7.0

- 新增：用例执行结果支持 `Actual Result`，用于记录真实观察到的执行结果。
- 新增：测试计划支持上传、预览和删除执行截图，截图保存到 `test-runs/screenshots/<run-id>/`。
- 新增：HTML 测试报告展示实际结果和截图证据名称，便于失败/阻塞用例追溯。
- 新增：项目内置 `casebook-test-process-record` AI Agent 技能包，指导 AI 基于需求、YAML 用例、`test-runs/*.json`、截图和用户模板生成测试过程记录。
- 新增：提供 `docs/templates/l2-test-process-record.md` 作为测试过程记录参考模板，并同步到 `casebook init` 项目脚手架。
- 优化：`casebook serve` 顶部导航增加 GitHub 入口。
- 优化：移除侧栏 `Refresh` 按钮，依赖文件监听和自动刷新保持页面同步。
- 测试：补充执行截图上传、预览、删除和项目脚手架技能文件的单元测试。

## 0.6.0

- 新增：测试计划支持 `Full run` 和 `Retest failed/blocked/deferred` 两种执行模式。
- 新增：支持基于上一轮失败、阻塞、延期用例创建下一轮重测计划。
- 新增：执行结果增加 `Deferred`，用于标记需要延后到下一轮验证的用例。
- 优化：测试计划未全部执行时，不允许完成计划。
- 优化：测试计划创建区域区分 `Full run` 和 `Retest`，来源计划只在重测模式下显示。
- 优化：测试计划详情中，执行信息与评审标记互斥展示。
- 优化：报告按测试计划实际范围统计，支持重测轮次结果展示。

## 0.5.0

- 新增：`casebook export` 命令，支持将 YAML 用例导出为独立 HTML 文件。
- 新增：导出 HTML 支持目录聚合、单文件导出、`--tag` 和 `--priority` 过滤。
- 新增：静态 HTML 支持评审标记、备注和本地保存，方便会议评审和冒烟用例分发。
- 优化：`casebook export` 与 `casebook serve` 的用例列表、展开样式、标签样式保持一致。
- 优化：统一 Mark 交互，标记后展示标签，不再整行变色。
- 优化：用例详情中的 Preconditions、Steps、Expected Results 样式与本地工作台保持一致。

## 0.4.0

- 新增：`casebook renumber` 命令，支持按当前 YAML 文件顺序重新整理用例 ID。
- 新增：页面增加 `ID Update` 能力，用例评审后可快速修正编号顺序。
- 新增：测试计划模式支持按执行结果筛选用例。
- 新增：执行详情恢复 `Defects` 字段，支持记录 JIRA 或缺陷链接。
- 优化：命令行输出引入字符画 Banner，展示版本、访问地址、范围、用例数量等信息。
- 优化：引入 `loguru` 统一日志输出。
- 优化：项目中的用户提示统一为英文。

## 0.3.0

- 新增：`casebook report <run-file>` 命令，支持生成 HTML 测试报告。
- 新增：报告包含测试计划信息、执行概览、失败用例和阻塞用例。
- 新增：报告引入图表统计，展示通过率和执行状态分布。
- 新增：`casebook serve` 页面 favicon。
- 优化：测试计划信息展示，支持环境、测试人员、完成时间等基础信息。
- 优化：完成测试计划时自动写入完成时间，便于生成报告。

## 0.2.0

- 新增：`casebook init` 项目脚手架，生成 Schema、示例用例、技能包和编辑器配置。
- 新增：引入 AI Agent 技能包，指导 AI 根据需求生成符合 Schema 的 YAML 用例。
- 新增：CLI 改为 Typer 实现，命令结构更清晰。
- 新增：测试计划最小可用能力，支持创建、选择和保存执行轮次。
- 新增：`test-runs/*.json` 存储执行结果。
- 新增：用例支持 `Passed`、`Failed`、`Blocked` 执行状态。
- 新增：顶部进度条和执行统计卡片。

## 0.1.0

- 新增：首个可用的 `casebook serve` 本地工作台。
- 新增：扫描 `releases/` 下的 YAML 测试用例并展示文件树。
- 新增：用例列表、详情展开、优先级、类型、标签展示。
- 新增：用例 Mark 标记能力，支持在评审中记录需要更新的用例。
- 新增：轻量编辑能力，可在页面中修改用例内容并写回 YAML。
- 新增：文件监听能力，YAML 变更后页面可自动刷新。
